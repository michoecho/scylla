/.cache
/.direnv
/.envrc
/.local
/out
/compile_commands.json
/.vscode

# Nix build result symlink (e.g. from `nix build .#code`)
/result

# Bytecode from the Python helpers in tools/
__pycache__/

# AFL++ fuzzer output (queue, crashes, stats). The seed corpus under
# fuzz/*/in is kept; everything the fuzzer generates lives in fuzz/*/out.
/fuzz/*/out
